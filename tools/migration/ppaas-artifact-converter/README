------------------------------
PPaaS Artifact Conversion Tool 4.0.0 to 4.1.0
------------------------------

PPaaS Artifact Conversion Tool is a tool to convert artifact JSON files of 4.0.0 to 4.1.0


This tool will enable users to import artifact JSON files from PPaaS 4.0.0 and convert them to artifact JSON files of PPaaS 4.1.0

# Instructions

1. Build the ppaas-artifact-converter tool by running the following command

mvn package

3. Extract the generated ppaasArtifact-1.0-SNAPSHOT.zip 

2. Add the default values to the templates folder, if needed.

4. Run the run.sh in bin folder
